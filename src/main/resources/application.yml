server:
  port: 8080

spring:
  jpa:
    database: POSTGRESQL
    hibernate.ddl-auto: create
    show-sql: true

  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/botica
    username: postgres
    password: example